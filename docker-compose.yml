services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
        VITE_PROPERTIES_POLL_MS: ${VITE_PROPERTIES_POLL_MS:-10000}
    ports:
      - "80:80"
    restart: unless-stopped
